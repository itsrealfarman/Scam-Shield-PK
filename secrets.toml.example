# Copy this file to .streamlit/secrets.toml for local testing (never commit the real one).
# On Streamlit Cloud: App -> Settings -> Secrets, paste the same line there.

GROQ_API_KEY = "your_groq_api_key_here"
